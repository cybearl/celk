/**
 * The benchmark for the `Bech32Encoder` class.
 * @param bech32EncoderBenchmarkInputSize The size of the input for the cache benchmark.
 * @param benchmarkDuration The duration of the benchmark.
 */
export default function executeBech32EncoderBenchmark(
    bech32EncoderBenchmarkInputSize: number,
    benchmarkDuration: number
) {}
