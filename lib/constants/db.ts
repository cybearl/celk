/**
 * The path where an authentication verification is redirected to (login route).
 */
export const REDIRECT_TO = "/login"

/**
 * The maximum length of a version.
 */
export const MAX_VERSION_LENGTH = 8

/**
 * The maximum length of a URL.
 */
export const MAX_URL_LENGTH = 255

/**
 * The maximum length of an external user ID.
 */
export const MAX_EXTERNAL_USER_ID_LENGTH = 128

/**
 * The minimum length of an email.
 */
export const MIN_EMAIL_LENGTH = 8

/**
 * The maximum length of an email.
 */
export const MAX_EMAIL_LENGTH = 255

/**
 * The minimum length of a username.
 */
export const MIN_USERNAME_LENGTH = 4

/**
 * The maximum length of a username.
 */
export const MAX_USERNAME_LENGTH = 32

/**
 * The minimum length of a password.
 */
export const MIN_PASSWORD_LENGTH = 8

/**
 * The maximum length of a password.
 */
export const MAX_PASSWORD_LENGTH = 255

/**
 * The maximum length of the name column for all tables.
 */
export const MAX_NAME_LENGTH = 64

/**
 * The maximum length of the description column for all tables.
 */
export const MAX_DESCRIPTION_LENGTH = 255

/**
 * The maximum length of the address column for all tables.
 */
export const MAX_ADDRESS_LENGTH = 128

/**
 * The maximum length of the native currency column for all tables.
 */
export const MAX_NATIVE_CURRENCY_LENGTH = 16
